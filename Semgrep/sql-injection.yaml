rules:
  - id: sql-injection
    message: Potential SQL injection. Use parameterized queries.
    languages: [csharp]
    severity: CRITICAL
    patterns:
      - pattern: |
          $QUERY = "...$USER_INPUT..."
      - pattern-not: |
          SqlParameter("...", ...)